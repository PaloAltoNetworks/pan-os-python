
[pantag]

param.verbose         = <bool>
    * Set modular alert action logger to verbose mode
    * Defaults to "false"

param._cam = json
    * Prevent splunk startup and btool from erroring

[panwildfiresubmit]

param.verbose         = <bool>
    * Set modular alert action logger to verbose mode
    * Defaults to "false"

param._cam = json
    * Prevent splunk startup and btool from erroring
